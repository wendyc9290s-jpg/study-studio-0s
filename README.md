# AF Command Center (Phase 1)

## Run locally
Open `index.html` directly in a browser, or serve the folder with a static server.

## SPA routing note
If deployed with BrowserRouter-style deep links, production hosting must include SPA fallback/rewrite rules so routes such as `/study/sim-uob` resolve to the main app entry on refresh.

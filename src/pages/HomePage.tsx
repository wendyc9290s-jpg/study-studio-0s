import { PlaceholderPage } from '../components/PlaceholderPage';

export function HomePage() {
  return (
    <PlaceholderPage
      title="Home"
      description="Aggregated dashboard. Will show today's study tasks, current 4-month phase, progress across all subjects, energy check-in, and recent brain dump entries."
      versionTag="v0.2"
    />
  );
}
